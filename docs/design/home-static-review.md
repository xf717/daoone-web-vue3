# H01 首页静态迁移记录 · 2026-09-21

视觉源：Figma AiKnRY064P8q5Sb5k6Ksya / 411:52，用户认可的 Make 风格；业务源为 engineering 的 H01 原型。本轮只改首页，不改 Make 源、原型业务、workspace、登录或移动版。

## 已落实

- 首页独立主题：浅青灰 #F1F4F3、白卡、墨色 #0F1518、深青 #2C6F66 点缀；未修改全局主色。卡片16px圆角，输入和按钮48px高、12px圆角、15px文字。输入边界增强到 #83958F；不宣称全站可访问性认证。
- Nuxt UI真实选择器、公历/农历/四柱输入、更多设置、日期/时间/地点/干支校验。首访隐藏结果；生成后全宽显示；修改或编辑输入隐藏旧结果。示例按钮只填入资料，需另行生成。
- 问题切换分析、独立记录四阶段、三维对照与依据展开、FAQ、导航菜单、说明弹窗和页脚入口。对照支持方向键/Home/End。
- 登录、完整细盘、个人档案、个人提问、研究工作区入口显示明确的迁移边界，未虚构业务完成。移除 Make 的“永久免费”承诺。

## 检查与修正

两位独立 AI 只读代码评审，主代理修复：跨版块状态耦合、结果导致深色示例卡拉长、页脚入口缺失、对照键盘行为缺失、次按钮密度不一。浏览器实测1440与1366，22项定向检查通过，含48px控件、错误关联、生成/修改、全宽结果、弹窗Escape及焦点返回、各版块切换、1366无横向溢出。

`npm run lint`、`npm run typecheck`通过；既有Vitest 3个文件25项通过（既有测试不是本页视觉验收）。浏览器脚本 `home-static-check.js`；截图 `home-static-initial.png`、`home-static-generated.png`、`home-static-1366.png`。

## 明确边界

这是首页静态交互实现，尚未用户最终验收，不是生产业务联调。日期输入生成固定教学盘；四柱仅呈现输入，不验证历法组合；高级设置不执行时间换算。无网络提交、AI、真实排盘、付费或登录。草稿当前保留在页面状态中，刷新会重置；原型的跨页草稿恢复、认证后返回及专业细盘需随相关页面迁移。没有模拟异步加载；本地呈现同步完成。未测试移动版、完整读屏及生产构建。
